# 개발일지 2026-06-24

## 작업 환경
- 마이오노트 (Next.js 16 App Router + Supabase) / **Vercel 서버리스** (`myonote.vercel.app`)
- 개발 세션 모델: Claude Opus 4.8 / AI 기능 모델: **Upstage Solar(solar-pro2)** + **Upstage Information Extract**

> AI 근시관리 플랫폼화 2차. 전날 AI 요약(1번)·Vercel 이전에 이어 **검사 해설(2번)**, **굴절 부호 모델 정정**, **검사지 OCR 자동입력(3번)** 을 구현.

---

## 1. 검사 결과 즉시 해설 (AI 기능 2번)
검사기록 카드별 **"AI 해설"** 버튼 — 그 검사 한 건을 직전·또래 대비로 쉽게 풀어줌.

- `lib/aiReport.ts` `buildExamExplainContext()`: 검사 1건의 안축장 백분위 + 직전 검사 대비 증감 계산(기존 로직 재사용)
- `app/api/exam-explain`: Solar로 **핵심 내용별 불렛**(소제목+설명) 생성
- 안전: 정상범위·평균치 등 **미제공 수치 창작 금지**, 또래 비교는 안축장 백분위에만 한정(굴절은 값·변화만)
- UX 반복: 줄글→이모지→(이모지 남발 지적)→**깔끔한 불렛**으로 정착

## 2. 굴절 입력 부호 모델 정정 (정확도 기반 작업)
plus-cylinder 표기·큰 난시 등으로 **Sph가 +가 되는 처방**(예: Sph +1.00 / Cyl −4.00, SEQ −1.00)이 실재 → 기존 "Sph·Cyl 무조건 음수 저장" 모델은 광학적으로 부정확했음.

- **minus-cyl 표준 + 부호 있는 Sph**로 정정: Cyl은 음수 고정(minus-cyl), Sph는 ±토글로 실제 부호 저장
- `calcSeq` 실제 부호 기반(`Sph − Cyl크기/2`), 저장/편집 부호 보존
- 카드에 양수 Sph·SEQ는 `+` 표기(`fmtSigned`)
- SEQ는 표기 변환에 불변 → 기존 데이터·분석 영향 없음

## 3. 검사지 사진 OCR 자동입력 (AI 기능 3번)
장비가 다른 **안축장 검사지·굴절 검사지를 분리 업로드** → 각각 인식해 폼 자동 채움.

- `app/api/exam-extract`: **Upstage Information Extract**(`/v1/information-extraction`, model `information-extract`) — 검사지 종류별 JSON 스키마로 추출
  - axial: 안축장 R/L + 검사일 / refraction: S·C R/L(부호 포함) + 검사일
- `lib/examExtract.ts`: 사진 캔버스 축소(Vercel 본문 4.5MB 대비)→추출→**minus-cyl 정규화(plus-cyl이면 transpose)+부호 있는 Sph**로 폼 매핑
- 모달 상단 "검사지 사진으로 자동 채우기" 업로드 버튼 2개. **저장 전 사용자 검토 필수**(자동저장 안 함)
- 디버깅: IE는 `content`에 이미지 1개만 허용 / 1차 속성 type에 nullable 유니온 불가 → 단일 타입으로 수정
- 실제 검사지로 안축장·굴절 정확 인식 확인 ✅ (※ 페이지당 과금)

---

## 4. 개인정보 고지 (AI 외부처리)
AI 요약·검사지 OCR이 외부 처리사(Upstage)로 데이터를 전송하므로 방침·고지 반영.
- `/privacy` 6조 위탁에 Upstage(① 요약=식별정보 미포함 / ② 검사지 이미지=인쇄정보 포함 가능·미저장 구분)·Vercel 추가, 수집항목·이용목적·수정일 갱신
- 검사지 업로드·AI 요약 카드에 "외부 AI(Upstage) 전송" 고지문
- ⚠️ Upstage 데이터 보관·학습 정책은 미검증 → "수탁사 정책 따름"으로만 표기(운영 전 확인 필요)

## 5. 검사기록 모달 UI 개편
- 사진 안내 1줄+ⓘ, 안축장/굴절 카드 그룹핑, 메모만 "추가 정보(선택)" 접기(다음 예약일은 노출 유지)
- 구조 재편: **고정 헤더(teal-500 솔리드, 검사일/안과)** + 스크롤 본문 + **하단 고정 저장 바**
- 안축장 우안/좌안 한 줄 배치(굴절과 라벨 통일), 검사지 버튼 업로드 아이콘

## 6. AI 기능 4번 — 부모용 근시 Q&A 챗봇
- 홈 우하단 챗 버블 → 전체화면 챗 오버레이(`ChatSheet`). `api/chat`(Solar 멀티턴)
- **우리 아이 데이터 그라운딩**: `buildReportContext`(익명) 전송 → "우리 아이 진행 빠른편?"에 실제 숫자로 답
- 안전 가드(번호 규칙): 질문에 직접 답·범위 밖 거절·진단 금지·제공 숫자만·전문의 안내
- ※ 답변 품질(질문 적합도 등) 추가 개선 필요 — 다음 과제. 답변 길이 선택 기능은 넣었다가 제거(자세한 설명 기본)

## 7. 버그 수정 — 짧은 검사간격 연간 환산(×12)
- 검사 2건이 짧은 간격(예 0.2개월)일 때 연간 환산이 수십 배로 뻥튀기되던 문제
- `buildReportContext`: 간격 3개월 미만이면 `annualized=null`. 챗/요약/해설 프롬프트에 "임의 연간 환산 금지" 추가 → 라이브 기능들도 함께 수정됨

---

## 다음 세션 이어서 할 일
- **챗봇 품질 개선**: 질문 적합도·톤·길이 등 (이번 세션에서 라이브했으나 더 다듬기로 함)
- 남은 AI 기능: **5번 생활습관 코칭/넛지**, **6번 진행 추세 알림**(의료리스크 신중)
- (운영) **Upstage 데이터 정책 검증** 후 방침 강화 / plus-cyl OCR 검증 / 커스텀 도메인
