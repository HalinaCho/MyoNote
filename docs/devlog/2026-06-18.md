# 개발일지 2026-06-18

## 작업 환경
- 프로젝트: 마이오노트 (Next.js 16 + Supabase + TypeScript)
- 배포: GitHub Pages (`https://HalinaCho.github.io/MyoNote`)

---

## 오늘 완료한 작업

### 1. 보호자 목록 표시 및 제거 기능 (설정 탭)

#### Supabase RPC 함수 (`get_child_guardians`)
- `SECURITY DEFINER` 함수로 `auth.users`에서 이름/이메일 조회
- 호출자가 해당 자녀의 보호자인지 내부 검증
- 역할 정렬: owner → editor → viewer 순
- 표시 이름 우선순위: `full_name` → `name` → `preferred_username` → 이메일 앞부분

#### DELETE RLS 정책 추가 (`eyebody_child_guardians`)
- 소유자: 비소유자 보호자 제거 가능
- 본인: 소유자가 아닌 자신의 기록 삭제 가능 (나가기)

#### queries.ts 신규 함수
- `fetchGuardians(childId)` — RPC 호출, Guardian[] 반환
- `removeGuardian(childId, userId)` — 보호자 레코드 삭제

#### 설정 탭 UI (`src/app/dashboard/settings/page.tsx`)
- 보호자 섹션에 목록 표시 (로딩 중 animate-pulse 스켈레톤 2행)
- 역할 배지: 소유자(teal) / 편집자(amber) / 열람자(gray)
- "나" 표시: 현재 로그인 사용자 강조
- 소유자 → 비소유자에 "제거" 버튼 표시
- 비소유자 본인 → "나가기" 버튼 표시
- 제거/나가기 후 목록 즉시 갱신

---

### 2. PWA 설정

#### manifest.json (`public/manifest.json`)
- `name`: 마이오노트, `short_name`: 마이오노트
- `start_url` / `scope`: `/MyoNote/`
- `display`: standalone, `orientation`: portrait
- `background_color`: `#edf7f6`, `theme_color`: `#10bcad`
- 아이콘: `icon-192.png` / `icon-512.png` (purpose: "any")

#### Service Worker (`public/sw.js`)
- 캐시 전략 3종 분리:
  - Supabase API (`*.supabase.co`): 네트워크 전용 (캐시 안 함)
  - Next.js 정적 청크 (`/_next/static/`): 캐시 우선 (콘텐츠 해시 → 영구 캐시)
  - 앱 페이지: 네트워크 우선, 실패 시 캐시 폴백
- `install` 시 `skipWaiting()`, `activate` 시 이전 캐시 삭제 + `clients.claim()`

#### SwRegister 컴포넌트 (`src/components/SwRegister.tsx`)
- 클라이언트 컴포넌트, SSR 안전
- `navigator.serviceWorker.register('/MyoNote/sw.js', { scope: '/MyoNote/' })`

#### layout.tsx 업데이트
- `metadata.manifest`: manifest.json 경로 추가
- `metadata.appleWebApp`: capable/statusBarStyle/title 설정 (iOS)
- `icons.apple`: icon.png → apple-touch-icon 적용
- `<SwRegister />` body 하단 삽입

#### PWA 아이콘 교체
- `MyoNote Icon(mint,512x512).png` → 라운드 모서리(r=90, 약 17.5%) 처리
- PowerShell `System.Drawing` 으로 투명 배경 + 라운드 클리핑
- `public/icon-512.png` (512×512), `public/icon-192.png` (192×192) 생성

---

### 3. 로딩/에러 UI 통일

#### 공통 컴포넌트 신규

**`src/components/ui/TabSkeleton.tsx`**
- 카드 3개 animate-pulse 스켈레톤
- 제목 바(1/4너비) + 본문 2행(full / 3/4너비) 구성
- 모든 탭 로딩 상태에 공통 사용

**`src/components/ui/EmptyState.tsx`**
- `message` + optional `action: { label, onClick }` props
- 흰 카드(`bg-white rounded-2xl p-8 shadow-sm`) 스타일 통일
- action 있을 때 teal 텍스트 버튼 표시

#### 각 탭 적용

| 탭 | 변경 내용 |
|---|---|
| 홈 | `"불러오는 중..."` 텍스트 → `<TabSkeleton />` |
| 기록 | `isLoading` 추가 → `<TabSkeleton />`, empty state → `<EmptyState>` + "첫 기록 추가" 액션 |
| 캘린더 | `isLoading` 추가 → `<TabSkeleton />` |
| 안축장 | `isLoading` 추가 → `<TabSkeleton />`, empty state → `<EmptyState>` |
| 굴절도수 | `isLoading` 추가 → `<TabSkeleton />`, empty state → `<EmptyState>` |
| 달성률 | `isLoading` 추가 → `<TabSkeleton />`, empty state → `<EmptyState>` |

---

### 4. 홈 케어달성률 주간 스트립 → 캘린더 이동

- 홈 대시보드 "케어 달성률" 섹션의 7일 주간 스트립 클릭 시 캘린더 탭(`/dashboard/calendar`)으로 이동
- `useRouter().push('/dashboard/calendar')` + `cursor-pointer` 적용

---

### 5. 캘린더 페이지에 달성률 차트 통합

- `ComplianceTab` 컴포넌트를 캘린더 페이지 하단에 추가
- 캘린더 그리드 아래 스크롤하면 6개월 막대 차트 + 기간 통계 확인 가능
- 분석 탭에서 "달성률" 서브탭 제거 → 안축장 / 굴절도수 2탭만 유지

---

## 수정된 주요 파일
- `public/manifest.json` — 신규: PWA 메니페스트
- `public/sw.js` — 신규: 서비스 워커
- `public/icon-512.png` — 신규: 민트 배경 라운드 PWA 아이콘 512px
- `public/icon-192.png` — 신규: 민트 배경 라운드 PWA 아이콘 192px
- `src/components/SwRegister.tsx` — 신규: 서비스 워커 등록 컴포넌트
- `src/components/ui/TabSkeleton.tsx` — 신규: 공통 로딩 스켈레톤
- `src/components/ui/EmptyState.tsx` — 신규: 공통 빈 데이터 상태
- `src/app/layout.tsx` — manifest/appleWebApp/apple-touch-icon 추가, SwRegister 삽입
- `src/app/dashboard/page.tsx` — 로딩 UI 개선, 주간 스트립 클릭 → 캘린더 이동
- `src/app/dashboard/records/page.tsx` — isLoading + TabSkeleton + EmptyState
- `src/app/dashboard/calendar/page.tsx` — isLoading + TabSkeleton, ComplianceTab 추가
- `src/app/dashboard/analytics/page.tsx` — 달성률 탭 제거, 2탭만 유지
- `src/app/dashboard/settings/page.tsx` — 보호자 목록/제거/나가기 UI
- `src/lib/supabase/queries.ts` — fetchGuardians, removeGuardian 추가
- `src/components/analytics/AxialTab.tsx` — isLoading + EmptyState
- `src/components/analytics/SerTab.tsx` — isLoading + EmptyState
- `src/components/analytics/ComplianceTab.tsx` — isLoading + EmptyState

---

## 다음 이어할 작업 후보
- [ ] E2E 테스트 — 로그인~대시보드 핵심 흐름
