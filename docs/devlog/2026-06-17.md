# 개발일지 2026-06-17

## 작업 환경
- 프로젝트: 마이오노트 (Next.js 16 + Supabase + TypeScript)
- 로컬: `http://localhost:3000`

---

## 오늘 완료한 작업

### 1. 안축장 백분위 기능 추가 (`분석 > 안축장`)

#### 참조 데이터 및 계산 로직 (`src/lib/axialPercentile.ts` 신규)
- 동아시아 소아 코호트 기반 안축장 참조 데이터 (6~13세, P10/P25/P50/P75/P90)
  - 기존 MVP(Eyebody 프로젝트)와 동일한 수치 적용
- `calcAgeYears(birth, examDate)` — 검사 시점 나이(소수) 계산
- `calcPercentile(al, age)` — 밴드 보간법으로 백분위 산출 (P3~P97)
- `pctLabel(pct)` — 상위/하위/정상 범위 레이블 반환
- `normCurve(key)` — 차트용 스무스 참조선 포인트 배열 (나이 0.25 단위)

#### 또래 비교 차트
- X축: 나이(세), Y축: 안축장(mm) — 날짜 기반 → 나이 기반으로 전환
- P10 / P25 / P50 / P75 / P90 참조선 표시 (점선)
- P25–P75 구간 teal 채움 밴드
- 아이 실측 포인트(우안·좌안) 겹쳐 표시
- X축 범위: 오늘 기준 현재 나이 ±3세, 정수 눈금

#### 또래 비교 요약 카드
- 최근 검사 기준 OD/OS 백분위 표시
- 브랜드 teal(`#10bcad`) 컬러로 통일 — 상위/하위/정상 구분 없이 단일 색상
- PctBar: 0~100 트랙 위에 닷 마커로 백분위 위치 시각화

#### 뷰 구조 변경
- 기존: 변화 추이 / 또래 비교 토글
- 변경: 두 뷰를 한 페이지에 순서대로 통합 (토글 제거)

---

### 2. 안축장 변화 추이 차트 개선

- **Y축 고정**: 우안·좌안 개별 토글 시에도 전체 데이터 기준 Y 범위 유지
- **커스텀 범례 토글 버튼** (Chart.js 기본 범례 제거):
  - 활성 우안: teal 테두리 + teal 점
  - 활성 좌안: gray 테두리 + gray 점
  - 비활성: 배경·테두리 제거, 점·텍스트 연한 gray로 페이드

---

### 3. 굴절도수(SEQ) 차트 동일하게 개선

- Y축 고정 (전체 데이터 기준)
- 커스텀 범례 토글 버튼 (안축장과 동일 디자인)

---

### 4. 알림/리마인더 기능 추가

#### 알림 설정 저장 (`src/lib/notificationPrefs.ts` 신규)
- `getAlertDay()` / `setAlertDay()` — localStorage 기반 단일 알림 기준일 저장
- 설정값: 1일 전 / 3일 전 / 7일 전 중 하나 (단일 선택, 재클릭 시 해제)

#### 설정 탭 알림 섹션 (`src/app/dashboard/settings/page.tsx`)
- "알림 설정" 섹션 신규 추가 (병원 예약 알림)
- `faBell` 아이콘 + 칩 3개(1일 전 / 3일 전 / 7일 전) 단일 선택 UI
- 선택된 칩: teal 배경·테두리, 미선택: gray

#### 홈 대시보드 알림 배너 (`src/app/dashboard/page.tsx`)
- **D-day 배너**: 설정한 기준일 이내 도달 시 최상단 표시
  - D-0~D-3: rose 배경 (`bg-rose-50`)
  - D-4+: amber 배경 (`bg-amber-50`)
  - `faHospital` 아이콘, X 버튼으로 세션 내 dismiss
- **케어 미입력 배너**: 오늘 케어 기록 없을 때 표시
  - amber 배경, `faBell` 아이콘, X 버튼으로 dismiss

#### 다음 예약일 카드 D-day 배지 컬러 통일
- D-3 이하 → rose (`bg-rose-50 text-rose-500`)
- D-4~D-7 → amber (`bg-[#fde68a]/40 text-amber-600`)
- D-8 이상 → teal (`bg-teal-50 text-[#10bcad]`)
- 배너 컬러 기준과 통일

---

### 5. 컬러팔레트 메모리 저장

프로젝트 메모리(`memory/project_color_palette.md`)에 최종 확정 팔레트 기록:

| 역할 | 컬러 |
|---|---|
| 브랜드 / 성공 | `#10bcad` |
| 경고 / 주의 | `#fde68a` (amber-200) |
| 위험 / 나쁨 | `#fda4af` (rose-300) |
| 텍스트 | gray-800 / 600 / 400 |

- **블루 계열(blue-\*, sky-\*, indigo-\* 등) 사용 금지** — teal로 대체

---

## 수정된 주요 파일
- `src/lib/axialPercentile.ts` — 신규: 참조 데이터 + 백분위 계산 함수
- `src/components/analytics/AxialTab.tsx` — 또래 비교 통합, 커스텀 범례 토글
- `src/components/analytics/SerTab.tsx` — 커스텀 범례 토글, Y축 고정
- `src/lib/notificationPrefs.ts` — 신규: localStorage 알림 설정 저장
- `src/app/dashboard/settings/page.tsx` — 알림 설정 섹션 추가
- `src/app/dashboard/page.tsx` — D-day·케어 미입력 배너, 예약일 카드 배지 컬러 통일

---

### 6. 빈 상태 온보딩 플로우 (`src/components/onboarding/OnboardingFlow.tsx` 신규)

- 자녀 0명일 때 대시보드 빈 상태를 온보딩 플로우로 대체
- 환영 화면: 앱 아이콘 + 타이틀 + 핵심 기능 3종 카드 (근시 추적 / 병원 예약 알림 / 케어 달성률)
- 기능 카드 컬러: teal(근시 추적) / amber(예약 알림) / rose(케어 달성률)
- "자녀 등록하고 시작하기" 버튼 → 기존 `ChildFormModal` 재사용
- 자녀 추가 완료 시 ChildContext 갱신으로 온보딩 자동 종료, 대시보드 전환
- `dashboard/page.tsx`의 `!activeChild` 분기를 `<OnboardingFlow />`로 교체

---

## 수정된 주요 파일
- `src/lib/axialPercentile.ts` — 신규: 참조 데이터 + 백분위 계산 함수
- `src/components/analytics/AxialTab.tsx` — 또래 비교 통합, 커스텀 범례 토글
- `src/components/analytics/SerTab.tsx` — 커스텀 범례 토글, Y축 고정
- `src/lib/notificationPrefs.ts` — 신규: localStorage 알림 설정 저장
- `src/app/dashboard/settings/page.tsx` — 알림 설정 섹션 추가
- `src/app/dashboard/page.tsx` — D-day·케어 미입력 배너, 예약일 카드 배지 컬러 통일, 온보딩 연결
- `src/components/onboarding/OnboardingFlow.tsx` — 신규: 가입 후 빈 상태 온보딩 플로우

### 7. 네비게이션 5탭 재편 + 캘린더 탭 신규

#### 네비게이션 구조 변경 (`src/components/layout/BottomNav.tsx`)
- 4탭 → 5탭: **홈 | 캘린더 | 검사 | 분석 | 설정**
- 기존 "기록" 탭 → "캘린더"(faCalendarDays) + "검사"(faMicroscope) 로 분리

#### 캘린더 탭 신규 (`src/app/dashboard/calendar/page.tsx`)
- 기존 TreatmentTab 기반으로 분리 + 생활습관 통합
- **날짜 셀**: 케어 상태별 컬러(teal/amber/rose/gray) 유지, 하단에 faTree·faMobileScreen 아이콘 추가
  - 생활습관 기록 있음 → `text-gray-500` (진한 회색)
  - 기록 없음 → `text-gray-200` (연한 회색)
- **날짜 클릭 바텀시트**: 케어 토글 + 생활습관 TimeSpinner 통합 (저장 버튼 하나)
- 범례에 생활습관 아이콘 기준 추가

#### 검사 탭 단순화 (`src/app/dashboard/records/page.tsx`)
- 서브탭(캘린더·생활습관·안과검사) 제거
- 안과검사 기록만 직접 표시

---

---

### 8. GitHub Pages 배포 파이프라인 구축

#### 워크플로우 (`github/workflows/deploy.yml`)
- `main` 브랜치 푸시 시 자동 빌드 → `gh-pages` 브랜치 배포
- `peaceiris/actions-gh-pages@v4` 사용, `publish_dir: ./out`
- 빌드 환경변수: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` (GitHub Secrets), `NEXT_PUBLIC_SITE_URL=https://HalinaCho.github.io/MyoNote`
- `permissions: contents: write` 필수

#### next.config.ts 정적 빌드 설정
- `output: 'export'`, `images: { unoptimized: true }`
- `basePath: '/MyoNote'` (NODE_ENV=production일 때만)

#### 배포 URL
- `https://halinacho.github.io/MyoNote`

---

### 9. 카카오 OAuth 로그인 전체 수정

#### 문제 1 — KakaoLoginButton JSX 문법 오류 (빌드 실패 원인)
- `return` 내에 `<button>`, `<p>`, `<p>` 3개 요소를 fragment 없이 나열 → 빌드 자체가 실패
- `<>...</>` fragment로 감싸서 해결, 디버그 텍스트 제거

#### 문제 2 — Supabase 클라이언트 교체
- `@supabase/ssr` `createBrowserClient` (쿠키 기반) → `@supabase/supabase-js` `createClient` (localStorage 기반)
- 정적 사이트에서 쿠키 스토리지가 페이지 간 PKCE code verifier를 유실하는 문제 근본 해결

#### 문제 3 — auth/callback 이중 실행
- `useSearchParams` 의존성으로 effect가 2회 실행 → 첫 번째 `exchangeCodeForSession` 성공 후 URL이 바뀌면서 두 번째 실행이 auth_failed로 리다이렉트
- `useRef(false)` 가드로 1회 실행 보장
- `window.location.search`에서 직접 code 파라미터 추출 (useSearchParams 우회)

#### 문제 4 — Implicit flow 대응
- Supabase가 `#access_token=` 해시 방식(implicit)으로 응답하는 케이스 추가 처리
- `?code=` 있으면 PKCE → `exchangeCodeForSession`, 없으면 implicit → `onAuthStateChange` + `getSession` 대기

#### 문제 5 — 로그아웃 후 자동 로그인
- `prompt: 'login'` queryParam 추가 → 카카오 세션 살아있어도 항상 계정 입력 화면 표시

#### 수정된 리다이렉트 (basePath 누락 해결)
- `router.replace('/login')` → `window.location.replace(NEXT_PUBLIC_SITE_URL + '/login')`
- 적용 위치: `settings/page.tsx`, `DashboardShell.tsx`, `page.tsx`, `auth/callback/page.tsx`

---

### 10. 앱 아이콘 디자인 및 적용

#### 아이콘 디자인 (`public/icon.svg`)
- Claude Design에서 작업한 클립보드 + 눈 합성 아이콘 SVG 구현
- 1024×1024 viewBox 기반 (모든 크기로 스케일)
- 구성: 흰 배경(teal 테두리) + ccfbf1 클립보드 바디 + 흰 눈알 + teal 눈꺼풀 호 + 속눈썹 5개 + 홍채 링 + 클래스프(고리)
- 최종 컬러: bg=`#FFFFFF`, surface=`#ccfbf1`, eyeball=`#FFFFFF`, ink=`#10bcad`
- 테두리: `stroke="#10bcad" stroke-width="36"`, rect inset 18px으로 균일한 테두리 유지

#### 로그인 페이지 개선
- 아이콘 로고 교체: FA faEye → 커스텀 SVG 아이콘
- 이미지 경로: `NEXT_PUBLIC_SITE_URL + '/icon.svg'` (basePath 누락 방지)
- 아이콘 위치: 마이오노트 텍스트 바로 위, `mx-auto mb-2`
- 배경색: `bg-gray-50` → `bg-[#edf7f6]` (브랜드 연한 teal)

---

---

### 11. 파비콘 교체 및 개선

- `public/favicon.png` 교체 흐름:
  1. teal 배경 + icon.svg 동일 눈 디자인 64×64 PNG 생성 (sharp)
  2. 클립보드 제거, 눈만 teal 배경 중앙 배치 → viewBox 크롭으로 최대 크기
  3. 최종: Claude Design 파일(`MyoNote Favicon_claude design.png`) 직접 적용
- `layout.tsx` `icons.icon` / `icons.shortcut` → `favicon.png` 로 변경

---

### 12. 개인정보 처리방침 페이지 추가

- `src/app/privacy/page.tsx` 신규 생성
  - 수집 항목(카카오 ID·이메일·닉네임), 이용 목적, 보유 기간, 제3자 제공(Supabase·Kakao), 정보주체 권리, 보호책임자 섹션
- `src/app/login/page.tsx` — `<span>` → `<Link href="/privacy">` 교체
  - 기존: 클릭해도 아무 반응 없는 span → 실제 페이지 이동으로 개선

---

### 13. 헤더 디자인 개선

#### 앱 아이콘 + 브랜드 컬러 적용 (`src/components/layout/Header.tsx`)
- Claude Design 앱 아이콘(`public/icon.png`) 헤더 왼쪽에 배치
  - sharp `trim()` 으로 흰 여백 제거 (617×585 → 360×500)
  - `h-9 w-auto` (36px)로 클립보드+눈 아이콘 최대 활용
- `text-teal-600` → `text-[#10bcad]` 브랜드 컬러 통일
- 아이콘+텍스트 좌측 여백: 대시보드 카드와 동일 `px-4`(16px) 맞춤

#### 헤더 로고 홈 링크
- 아이콘+마이오노트 영역을 `<Link href="/dashboard">` 로 감싸서 어느 페이지에서든 탭 시 홈 이동

---

### 14. 안축장 백분위 차트 나이 범위 버그 수정

#### 버그 원인 (`src/components/analytics/AxialTab.tsx`)
- X축 범위 계산: `xMin = Math.max(6, curAge-3)`, `xMax = Math.min(13, curAge+3)`
- 자녀 나이가 16세 이상이면 `xMin > xMax` → 차트 범위 역전으로 빈 화면

#### 수정
- `xMin >= xMax` 조건 분기 추가: 참조 데이터 범위(만 6~13세) 밖이면 안내 메시지 표시
  > "또래 비교 기준 데이터는 만 6~13세까지 제공됩니다."

---

## 수정된 주요 파일 (오늘 추가)
- `public/favicon.png` — 파비콘 교체
- `public/icon.png` — 헤더용 앱 아이콘(여백 trim)
- `src/app/layout.tsx` — favicon.png 아이콘 메타데이터 적용
- `src/app/privacy/page.tsx` — 신규: 개인정보 처리방침 페이지
- `src/app/login/page.tsx` — 개인정보 처리방침 링크 연결
- `src/components/layout/Header.tsx` — 아이콘+브랜드컬러+홈 링크
- `src/components/analytics/AxialTab.tsx` — 나이 범위 밖 차트 버그 수정

---

## 다음 이어할 작업 후보
- [ ] 초대 코드 UI (설정 탭)
- [ ] PWA 설정 (manifest, service worker)
- [ ] 분석 탭 날짜 범위 필터
- [ ] 로딩/에러 UI 통일
- [ ] E2E 테스트
