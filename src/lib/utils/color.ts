// 브랜드 색 토큰 — docs/brand-palette.html 기준

// 부모 홈 히어로 배경. 팔레트에서 가장 깊은 teal(=teal-700)로 고정한다.
// 병원마다 다른 색을 면 전체에 입히지 않는 이유: 로고와 병원 이름이 이미 소속을 말해주고,
// 색이 매번 달라지면 그 위에 얹는 글자·배지 대비를 병원 수만큼 보증해야 한다.
// 병원 고유색은 히어로 상단 얇은 선으로만 남긴다.
export const HERO_BG = '#0f766e'

// 병원이 브랜드 컬러를 지정하지 않았을 때 쓰는 기본값(teal-500)
export const DEFAULT_BRAND_COLOR = '#14b8a6'
